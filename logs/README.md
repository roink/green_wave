# Logging artefacts

This directory stores timestamped execution logs automatically generated when
project scripts run. The logging hook mirrors all console output so that coding
agents can review dataset statistics and processing steps without accessing the
full raw data. Commit relevant `.log` files alongside code changes whenever they
capture new information about available datasets or preprocessing results.
